@model WebCRUDMVCSQL.Models.Pedidos

@{
    ViewData["Title"] = "Deletar Pedido";
}

<div class="row justify-content-center mt-3">
    <div class="col-md-8 col-lg-6">
        <div class="card border-0 shadow-sm rounded-3 p-4">
            <div class="card-body">
                <div class="mb-3 text-center text-danger">
                    <h3 class="fw-bold m-0">Excluir Pedido</h3>
                    <p class="text-muted small mt-1">Deseja realmente remover este pedido do sistema?</p>
                </div>
                
                <div class="alert alert-warning text-center py-2 small mb-4" role="alert">
                    ⚠️ Esta operação removerá permanentemente o item do histórico.
                </div>

                <div class="bg-light rounded-3 p-4 mb-4">
                    <dl class="row mb-0">
                        <dt class="col-sm-4 text-secondary small fw-bold mb-3">Pedido nº:</dt>
                        <dd class="col-sm-8 text-dark fw-medium mb-3">#@Model.Id</dd>

                        <dt class="col-sm-4 text-secondary small fw-bold mb-3">Cliente:</dt>
                        <dd class="col-sm-8 text-dark fw-medium mb-3">@Model.Cliente?.Nome</dd>

                        <dt class="col-sm-4 text-secondary small fw-bold mb-0">Total:</dt>
                        <dd class="col-sm-8 text-danger fw-bold mb-0">R$ @Model.Total.ToString("F2")</dd>
                    </dl>
                </div>

                <form asp-action="Delete">
                    <input type="hidden" asp-for="Id" />
                    <div class="d-flex justify-content-between align-items-center">
                        <a asp-action="Index" class="text-decoration-none fw-semibold text-muted">Cancelar</a>
                        <button type="submit" class="btn btn-danger px-4 py-2 fw-bold shadow-sm">Confirmar Exclusão</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>